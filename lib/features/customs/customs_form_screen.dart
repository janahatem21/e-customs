import 'package:flutter/material.dart';

class CustomsFormScreen extends StatelessWidget {
  const CustomsFormScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text('Customs Form Screen')),
    );
  }
}